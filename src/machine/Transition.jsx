// import { useContext, useEffect, useMemo } from 'react';
// import { MachineContext } from './Machine';
// import { StateNodeContext } from './State';

function Transition(props) {
    // const { cond, event, target } = props;
    // const { event: machineEvent, id: machineId } = useContext(MachineContext);
    // const { elevateTransition, id: parentId, path: parentPath, stack } = useContext(StateNodeContext);

    // useEffect(() => {
    //     if (machineEvent && machineEvent.event === event) {
    //         if (cond && cond === false) {
    //             return;
    //         }

    //         // elevateTransition(props);
    //     }
    // }, [ machineEvent ]);

    return null;
}

export default Transition;
