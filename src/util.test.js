import {
    deriveStateFromUrl,
    getChildStateNodes,
    getChildrenOfType,
    getInitialChildStateNode,
    injectUrlParams,
    isCurrentStack,
    isExactStack,
    isDynamicSegment,
    // isDynamicPath,
    isRootSemgent,
    isRootStack,
    isNotFound,
    normalizeChildStateProps,
    resolveInitial,
    resolveToAtomic,
    segmentize,
    selectTransition
} from './util';

describe('utility functions', () => {
    
});
